@model NorthwindWeb.Models.Region

@{
    ViewBag.Title = "Modify";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

    <h2>Modify</h2>
    <hr />
    <ol class="breadcrumb">
        <li>
            @Html.ActionLink("Dashboard", "Home", "Dashboard")
        </li>
        <li>
            @Html.ActionLink("Modify", "Index", "Regions")
        </li>
        <li class="active">Modify</li>
    </ol>


    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            <h4>Region</h4>
            <hr />
            @Html.ValidationSummary(true)
            @Html.HiddenFor(model => model.RegionID)

            <div class="form-group">
                @Html.LabelFor(model => model.RegionDescription, "Region description",
               htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.RegionDescription)
                    @Html.ValidationMessageFor(model => model.RegionDescription)
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Save" class="btn btn-default" />
                </div>
            </div>
        </div>
    }

    <div>
        @Html.ActionLink("Back", "Details", "Regions", new { id = Model.RegionID }, new { @class = "btn btn-default" })
        
    </div>


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
