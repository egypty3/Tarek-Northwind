@model NorthwindWeb.Models.Region

@{
    ViewBag.Title = "Remove";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

    <h2>Remove</h2>
    <hr />
    <ol class="breadcrumb">
        <li>
            @Html.ActionLink("Dashboard", "Home", "Dashboard")
        </li>
        <li>
            @Html.ActionLink("Regions", "Index", "Regions")
        </li>
        <li class="active">Remove</li>
    </ol>

    <h3>Surely you want to delete?</h3>
    <div>
        <h4>Region</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                Region Description
            </dt>

            <dd>
                @Html.DisplayFor(model => model.RegionDescription)<br />
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                @Html.ActionLink("Back", "Index", "Regions", new { @class = "btn btn-default" })
                <input type="submit" value="Remove" class="btn btn-default" /> 
            </div>
           
        }
       
    </div>
