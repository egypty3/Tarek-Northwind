@{
    ViewBag.Title = "Clienti";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}
<div class="row">
    <div class="col-lg-12">
        <h2>Customers</h2>
        <hr />
        <ol class="breadcrumb">
            <li>
                @Html.ActionLink("Dashboard", "Home", "Dashboard")
            </li>

            <li class="active">Customers</li>
        </ol>
        <hr />
        <br />
        <div>
            @Html.ActionLink("Add Customers", "Create", null, new { @class = "btn btn-default" })
        </div>
    </div>
</div>
<br />
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h4>
                    Customer List
                </h4>
            </div>
            <div class="panel-body">
                <table id="CustomersTable" class="cell-border hover order-column">
                    <thead>
                        <tr>
                            <th>
                                Company Name
                            </th>
                            <th>
                                Contact Name
                            </th>
                            <th>
                                Contact Title
                            </th>
                            <th>
                                City
                            </th>
                            <th>
                                Country
                            </th>
                            <th>
                                Phone
                            </th>

                            <th>
                                Remove
                            </th>
                        </tr>
                    </thead>

                    <tbody></tbody>

                </table>
            </div>
        </div>
    </div>
</div>
@*<div class="row text-center">
        <div class="col-lg-12">
            Pagina @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) din @Model.PageCount

            @Html.PagedListPager(Model, page => Url.Action("Index", "Customers", new { search = ViewBag.search, page }))
        </div>
    </div>*@
